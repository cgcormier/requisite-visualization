services:
  postgres:
    image: postgres:16-alpine
    container_name: requisite-visualization-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-requisite_visualization}
      POSTGRES_USER: ${POSTGRES_USER:-requisite_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?cool-ahh-password}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/db/migrations/001_initial_schema.sql:/docker-entrypoint-initdb.d/001_initial_schema.sql:ro
      - ./backend/db/seeds/001_sample_data.sql:/docker-entrypoint-initdb.d/002_sample_data.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  postgres_data:
